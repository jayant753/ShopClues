import { IoMdHeart } from "react-icons/io"; // Importing the heart icon from the react-icons library

// Defining the Wishlistbutton functional component
const Wishlistbutton = () => {
  return (
    <div>
      <IoMdHeart />
    </div>
  );
};

export default Wishlistbutton; // Exporting the Wishlistbutton component as the default export
